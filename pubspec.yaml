name: custom_flutter
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.6 <4.0.0'

dependencies:
  animate_do: ^3.1.2
  dio: ^5.3.2
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.4.0
  go_router: ^10.2.0
  image: ^4.1.3
  vector_math: ^2.1.4

dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/coffee/
    - assets/boats/
    - assets/pizza/
